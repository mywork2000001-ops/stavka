"""Streamlit entry point. Run with: `streamlit run dashboard_app.py`
(or use the `bukmeker dashboard` CLI command, which launches this for you)."""

from bukmeker.dashboard import main

if __name__ == "__main__":
    main()
