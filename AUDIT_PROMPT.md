# AUDIT & FIX PROMPT — Bukmeker (v7)

Полный аудит существующего проекта (математическое ядро, сущности, коннекторы,
дашборд, CLI) на корректность, безопасность, согласованность документации и
покрытие тестами — с обязательным исправлением найденного, а не только
перечислением. Выполняется автономно, без уточняющих вопросов пользователю.

## Роль
Ты — независимый ревьюер, который не писал этот код (даже если писал его в
предыдущей сессии) и проверяет его заново по каждому измерению ниже. Находка
без исправления и без теста, фиксирующего исправление, не считается закрытой.

## Измерения аудита

1. **Корректность математики** — перепроверить формулы в `features.py`,
   `ratings.py`, `models/goals.py`, `margin.py`, `calibration.py`,
   `value_betting.py`, `coupon.py`, `monetization.py`, `sports/*` на предмет
   пограничных случаев, не покрытых существующими тестами.
2. **Безопасность** — `auth.py` (сравнение пароля таймингово-безопасным
   способом), `connectors/*` (утечка ключей в логи/print, инъекции в URL),
   `dashboard.py` (не течёт ли пароль/ключи в интерфейс или session_state
   в открытом виде).
3. **Качество кода** — ruff чист, отсутствие мёртвого кода, консистентность
   сигнатур, отсутствие бессмысленных `except Exception: pass`.
4. **Покрытие тестами** — найти функции/ветки без прямого теста; особое
   внимание граничным условиям (пустые списки, нулевые/отрицательные входы,
   дублирующиеся id).
5. **Согласованность документации** — PROMPT.md/README.md/USAGE.md должны
   отражать актуальный код (число тестов, список команд, флагов CLI).
6. **Целостность данных** — `entities.py`: уникальность id, отсутствие дублей
   имён участников внутри одной лиги, консистентность seed-данных.

## Процесс
1. Прогнать `ruff check .` и `pytest tests/ -q` как базовую линию.
2. Пройти по каждому модулю целенаправленно (не по diff, а по всему файлу).
3. Каждую находку сформулировать как конкретный сценарий отказа (не общими
   словами "могло бы быть лучше").
4. Исправить, добавить/обновить тест, воспроизводящий сценарий из находки.
5. Повторный прогон `ruff` + `pytest` после каждой правки.
6. Обновить документацию, если правка меняет наблюдаемое поведение.
7. Зафиксировать в этом файле журнал находок и их статус (ниже).

## Журнал находок (раунд 1)

Базовая линия перед аудитом: `ruff check .` чист, `pytest tests/ -q` — 130 passed.
После исправлений: `ruff check .` чист, `pytest tests/ -q` — 143 passed.

| # | Файл | Сценарий отказа | Серьёзность | Статус |
|---|---|---|---|---|
| 1 | `dashboard.py` (coupon page) | Некорректная строка таблицы ставок (нечисловой `team_ids` и т.п.) тихо пропускалась без единого сообщения — пользователь не понимал, почему в купоне меньше ставок, чем строк введено. | medium | **Исправлено.** Логика разбора вынесена в чистую функцию `parse_bet_rows()` (тестируется напрямую, без Streamlit-рантайма), возвращает список пропущенных строк с причиной; UI показывает `st.warning` с деталями. 5 новых тестов (`test_dashboard_logic.py`). |
| 2 | `margin.py` | `multiplicative_margin_removal`/`shin_margin_removal` не проверяли `odds > 1` (в отличие от `value_betting.py`) — коэффициент 0 или отрицательный давал тихий NaN/Inf вместо ошибки. | medium | **Исправлено.** Добавлена `_validate_odds()`, общая для обеих функций. +2 теста. |
| 3 | `margin.py` (`shin_margin_removal`) | Фиксированная скобка `brentq` `[-0.999, 10.0]` могла не содержать корень для рынка с большим числом исходов и высоким overround → необработанная ошибка scipy. | medium | **Исправлено.** Скобка расширяется динамически до тех пор, пока не появится смена знака (с защитным пределом). +1 стресс-тест на 15 исходов с overround ~43%. |
| 4 | `auth.py` | Сравнение пароля через `==` — тайминговая утечка длины/содержимого пароля (низкий практический риск для локального dev-сервера, но неверная практика). | low | **Исправлено.** Заменено на `hmac.compare_digest`. Существующие тесты подтверждают отсутствие регрессии. |
| 5 | `calibration.py` (`expected_calibration_error`) | Реализация усредняла `|acc-conf|` по бинам без учёта их размера (через `sklearn.calibration_curve`) — не соответствует канонической формуле ECE (взвешенной по `n_m/N`), давая заниженную/искажённую оценку при неравномерных по размеру бинах. | medium | **Исправлено.** Переписано на ручное биннинг с явным взвешиванием по количеству наблюдений в бине. +1 тест, фиксирующий числовое значение (0.13 vs. невзвешенные 0.25). |
| 6 | `features.py` (`weighted_rolling_mean`) | Группа (например, команда) без единой строки истории до `as_of_date` полностью пропадала из результата `groupby` — последующий `.loc[team_id]` кидал `KeyError` вместо ожидаемого `NaN`. | medium | **Исправлено.** Результат переиндексируется по всем уникальным группам исходного `df`. +1 тест на команду без истории. |
| 7 | `ratings.py` (`PoissonStrength.fit`) | Команда в `home_ids`/`away_ids`, отсутствующая в `teams`, приводила к непрозрачному `KeyError` внутри `dict`-lookup вместо понятной ошибки. | medium | **Исправлено.** Явная проверка неизвестных команд с `ValueError`, перечисляющим конкретные имена. +1 тест. |
| 8 | `coupon.py` (`generate_coupons`) | Неограниченный комбинаторный перебор (`sum C(n, r)`) — большой список ставок (например, из дашборда с динамической таблицей) + высокий `max_events` мог зависнуть на минуты без обратной связи. | medium | **Исправлено.** Явный предохранитель на суммарное число комбинаций (200 000) с понятной ошибкой; дашборд перехватывает её через `st.error`, не падая. +2 теста (срабатывание предохранителя и разрешённая нагрузка под лимитом). |

Проверка целостности seed-данных `entities.py` (уникальность id, дубли имён
участников внутри лиги) выполнена программно в ходе аудита — нарушений не
найдено.

Отдельно проверено (без находок): утечка секретов в логи/print в
`connectors/*` — не обнаружена (используются параметры `requests`, а не
интерполяция строк в URL/заголовки).

## Журнал находок (раунд 2)

Углублённая проверка `connectors/*`, `value_betting.py`, `cli.py::run_dashboard`,
целостности `entities.py` (numeric-коды `pycountry`) и таймингов
`build_seed_registry()` — модулей, не разобранных детально в раунде 1.

Базовая линия: 143 passed. После исправлений: 149 passed.

| # | Файл | Сценарий отказа | Серьёзность | Статус |
|---|---|---|---|---|
| 9 | `connectors/schema.py` (`apply_mapping`) | `FieldMapping` обычно приходит из структурированного вывода LLM (`ClaudeFieldMapper`) — это untrusted-данные. Если модель "галлюцинирует" путь не строкой (int/list/dict вместо `"a.b.c"`), `get_by_path` падал на `path.split(".")` с `AttributeError`. | medium | **Исправлено.** Путь используется только если это непустая строка; иначе поле считается неразмеченным (`None`), без падения. +1 тест с тремя видами "галлюцинированных" путей. |
| 10 | `connectors/ai_mapper.py` (`infer_mapping`) | Пустой `message.content` (редкий, но реальный формат ответа Anthropic API) приводил к необработанному `IndexError` вместо понятной ошибки. | low | **Исправлено.** Явная проверка с `ValueError`, поясняющим причину. +1 тест. |
| 11 | `value_betting.py` (`overlay`, `probability_edge`) | В отличие от `expected_value`/`value_percentage`/`kelly_stake`, эти две функции не проверяли, что `model_prob`/`fair_prob` лежат в `[0, 1]` — например, случайная передача коэффициента вместо вероятности молча давала бессмысленное число вместо ошибки. | medium | **Исправлено.** Добавлена валидация `_validate_prob()` в обе функции. +2 теста. |
| 12 | `cli.py` (`run_dashboard`) | Проверка наличия Streamlit через `shutil.which("streamlit")` и вызов через голую команду `streamlit` — не находит интерпретатор-локальную установку, если каталог со скриптами окружения не в `PATH` (типичная ситуация в некоторых конфигурациях venv на Windows), даже когда пакет `streamlit` реально установлен и импортируется. | low | **Исправлено.** Проверка через `importlib.util.find_spec("streamlit")`, запуск через `[sys.executable, "-m", "streamlit", ...]` — не зависит от `PATH`. +2 теста (корректный запуск и explicit-отсутствие пакета). |

Проверено без находок: `sports/basketball.py`/`tennis.py`/`football.py`
(валидация уже присутствовала везде), `connectors/raw_source.py` и
`connectors/ai_connector.py` (нет инъекций, нет утечек), `connectors/sync.py`
(логика дедупликации уже покрыта раундом создания фичи), уникальность и
корректность numeric-кодов `pycountry` для всех 249 стран (проверено
программно), производительность `build_seed_registry()` (~1.1 мс на вызов —
пересоздание на каждый ререндер страницы «Страны и лиги» не является
проблемой, кеширование не требуется).

## Журнал находок (раунд 3)

Систематический grep всех `except`-блоков в проекте (проверка на тихое
проглатывание ошибок — ни одного необоснованного не найдено) плюс
целенаправленная проверка `models/goals.py`, которую раунды 1–2 не разбирали
напрямую.

Базовая линия: 149 passed. После исправлений: 152 passed.

| # | Файл | Сценарий отказа | Серьёзность | Статус |
|---|---|---|---|---|
| 13 | `models/goals.py` (все функции с параметрами `lambda`/`mu`) | Ни одна функция (`poisson_pmf`, `poisson_matrix`, `bivariate_poisson_matrix`, `dixon_coles_matrix`, `skellam_probs`, `monte_carlo_outcome_probs`, `negative_binomial_pmf`) не проверяла `lambda >= 0` — отрицательное значение (например, из ошибки в апстрим-расчёте рейтинга) тихо давало `NaN` по всей матрице/словарю вероятностей вместо ошибки. Проверено эмпирически: `poisson_matrix(-1.0, 1.0).sum()` → `nan`. | medium | **Исправлено.** Общая `_validate_lambda()`, применённая во всех перечисленных функциях (`lambda == 0` — легитимный вырожденный случай, не ошибка); `negative_binomial_pmf` также проверяет `dispersion > 0`. +3 теста (отрицательная λ везде отклоняется, нулевая λ — нет, `dispersion <= 0` отклоняется). |

Дополнительно проверено (без находок): полный `grep -rn "except"` по всему
пакету — все обработчики исключений точечные (конкретные типы) и либо
пробрасывают понятную ошибку, либо осознанно возвращают явное значение
(`None`/пропуск строки с причиной), тихого проглатывания не обнаружено.
Регрессионная проверка: `bukmeker demo` выполнен end-to-end после всех правок
раунда 3, числа в выводе не изменились.

## Журнал находок (раунд 4 — инфраструктура)

| # | Файл | Сценарий отказа | Серьёзность | Статус |
|---|---|---|---|---|
| 14 | `.github/workflows/ci.yml` vs. локальная ветка git | CI настроен на триггер `push`/`pull_request` в ветку `main`, но локальный репозиторий (созданный через `git init` в этой же сессии ранее) остался на ветке `master` — при публикации на GitHub в текущем виде GitHub Actions ни разу не запустился бы. | medium | **Исправлено.** Локальная ветка переименована `master` → `main` (`git branch -m`); remote не был настроен, переименование безопасно. Проверено: `git branch --show-current` → `main`. |

Проверено без находок: `pyproject.toml` — extras (`connectors`, `dashboard`,
`dev`) содержат ровно то, что реально требуется тестам и коду (включая
`anthropic`/`requests`/`streamlit`/`pycountry`); `.gitignore`/`LICENSE` без
замечаний.
